// Modules

// modules
import {
  handleScroll,
  handleMenu,
  handleAccordion,
  applyReveal,
  // @ts-ignore
} from '@modules'

// styles
import '@sass/styles.sass'

// Setup handlers

handleScroll()
handleMenu()
handleAccordion()

// Scroll reveal

applyReveal()
